<h1>検索結果</h1>

<% @recipes.each do |recipe| %>
  <div>
    <%= image_tag recipe.image %>
    <h2><%= link_to recipe.title, recipe_path(recipe) %></h2>
    <p><%= recipe.content.truncate(100) %></p>
  </div>
<% end %>
